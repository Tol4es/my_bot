[project]
name = "my_bot"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "python-telegram-bot>=21.6",
    "pydantic>=2.8",
    "python-dotenv>=1.0",
    "requests>=2.32",
    "deep-translator>=1.11",
]

[tool.pytests.ini_options]
pythonpath = ["."]